spring:
  application:
    name: eureka-server
#  config:
#    import: optional:configserver:http://localhost:8000
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
server:
  port: 8761
management:
    endpoints:
      web:
        exposure:
          include: "*"